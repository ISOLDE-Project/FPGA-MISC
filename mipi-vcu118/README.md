## Recreating the Block Design

To recreate the complete block design, copy the following files and directories into the same folder:

- `final_bd.tcl`
- `vivado_ip/`

Open Vivado, cd to this folder and, from the Tcl Console, run:

```tcl
source ./final_bd.tcl
